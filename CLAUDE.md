## Git

This repo uses the **sammiezx** GitHub account. The remote is `git@github-sammiezx:sammiezx/Noether.git` (SSH host alias `github-sammiezx`). Commit identity: `sammiezx <sameerpantz@gmail.com>`. Do not push via the default `github.com` remote — that authenticates as a different account and will be denied.
