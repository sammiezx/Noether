# Neural TTS helper env (Kokoro-82M). Installed into a SEPARATE Python 3.12
# venv (.venv-tts), NOT the main 3.14 app env — these wheels don't build on
# 3.14. See setup_neural_tts.sh, and tts.py / neural_tts_server.py for usage.
kokoro-onnx>=0.5.0
soundfile>=0.13.0
librosa>=0.11.0
phonemizer-fork>=3.3.0
# Voice conversion (knn-vc, loaded via torch.hub) for cloned character voices
# like Einstein/Batman — repaints clean Kokoro speech into a target voice.
torch>=2.11
torchaudio>=2.11
torchcodec>=0.14
noisereduce>=3.0
